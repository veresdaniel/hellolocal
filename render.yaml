services:
  - type: web
    name: hellolocal-frontend
    env: node
    rootDir: apps/web
    buildCommand: pnpm install && pnpm build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production

